language: node_js

node_js:
  - "node"

env:
  - workerCount=2 timeout=600000

branches:
  only:
    - dev
    - master

install:
  - npm install typescript
  - npm install

cache:
  directories:
    - node_modules
